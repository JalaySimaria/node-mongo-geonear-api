'use strict';

const DriversModel = require('./drivers');
const UsersModel = require('./users');
const OrdersModel = require('./orders');

module.exports = {
    DriversModel,
    UsersModel,
    OrdersModel
};
